# Generated by arkit8s generate-load-simulators
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: person-identity-availability-sim-1
  namespace: business-domain
  labels:
    app: person-identity-availability-sim-1
    arkit8s.simulator: "true"
  annotations:
    architecture.domain: business
    architecture.function: person-identity-load-simulator
    architecture.simulates: person-identity-verification-app-instance
    architecture.part_of: arkit8s
spec:
  replicas: 1
  selector:
    matchLabels:
      app: person-identity-availability-sim-1
  template:
    metadata:
      labels:
        app: person-identity-availability-sim-1
        arkit8s.simulator: "true"
        arkit8s.simulates: person-identity-verification-app-instance
    spec:
      containers:
        - name: availability-simulator
          image: registry.access.redhat.com/ubi10/ubi-minimal
          env:
            - name: BEHAVIOR
              value: "notready"
          command:
            - sh
            - -c
            - |
              echo "[$(date -u)] starting with BEHAVIOR=$BEHAVIOR"
              if [ "$BEHAVIOR" = "restart" ]; then
                sleep 60
                echo "[$(date -u)] simulating crash to trigger restart"
                exit 1
              elif [ "$BEHAVIOR" = "notready" ]; then
                while true; do
                  sleep 30
                done
              else
                while true; do
                  echo "[$(date -u)] running normally (BEHAVIOR=$BEHAVIOR)"
                  sleep 30
                done
              fi
          livenessProbe:
            exec:
              command:
                - sh
                - -c
                - exit 0
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            exec:
              command:
                - sh
                - -c
                - |
                  if [ "$BEHAVIOR" = "notready" ]; then
                    exit 1
                  else
                    exit 0
                  fi
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: person-identity-availability-sim-2
  namespace: business-domain
  labels:
    app: person-identity-availability-sim-2
    arkit8s.simulator: "true"
  annotations:
    architecture.domain: business
    architecture.function: person-identity-load-simulator
    architecture.simulates: person-identity-verification-app-instance
    architecture.part_of: arkit8s
spec:
  replicas: 1
  selector:
    matchLabels:
      app: person-identity-availability-sim-2
  template:
    metadata:
      labels:
        app: person-identity-availability-sim-2
        arkit8s.simulator: "true"
        arkit8s.simulates: person-identity-verification-app-instance
    spec:
      containers:
        - name: availability-simulator
          image: registry.access.redhat.com/ubi10/ubi-minimal
          env:
            - name: BEHAVIOR
              value: "ok"
          command:
            - sh
            - -c
            - |
              echo "[$(date -u)] starting with BEHAVIOR=$BEHAVIOR"
              if [ "$BEHAVIOR" = "restart" ]; then
                sleep 60
                echo "[$(date -u)] simulating crash to trigger restart"
                exit 1
              elif [ "$BEHAVIOR" = "notready" ]; then
                while true; do
                  sleep 30
                done
              else
                while true; do
                  echo "[$(date -u)] running normally (BEHAVIOR=$BEHAVIOR)"
                  sleep 30
                done
              fi
          livenessProbe:
            exec:
              command:
                - sh
                - -c
                - exit 0
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            exec:
              command:
                - sh
                - -c
                - |
                  if [ "$BEHAVIOR" = "notready" ]; then
                    exit 1
                  else
                    exit 0
                  fi
            initialDelaySeconds: 5
            periodSeconds: 10
